# 引思助手 项目状态

> 本文档是**会话切换时的交接入口**。每个阶段切换 / 新会话开始时，先读本文件，再去对应章节文档查权威细节。

**最后更新**：2026-05-21（B4 跨文档评审 + 全套修复 + PhaseNode prompt 整体审计：①概要 v0.1.1 增量补丁回补 OcrSchema 多题 + state subProblems / 双层循环路由 / 2 个 SSE chunk；②详设 §4.3 PhaseNode prompt B4 下沉 + §5.2 PhaseSignalParse 扩 5 信号 + §14.4 新增 E12/E13/E14 + §15 新增 D12/D13/D14 + §17 新增 DR-012/013 + §14.4.1 Judge Rubric → NFR-BUSINESS 映射；③PRD §10 加 5 条 P2 边界（流式发新消息禁用 / 多 tab / 会话超长 / SSE 重连 / 家长后台脱敏 P9）；④详设 §8.3 加异常 #14（OCR isSolvable=false）+ #15（OSS 50MB 预检）；⑤R-011 落地：OcrSchema 加 visualDescription + VisionNode prompt checklist + 详设 §17 DR-014；⑥PhaseNode prompt 整体审计 + 修复：新增 §4.3.0 共用骨架 / `formatVisualBlock` + `describeSubProblemContext` 工具函数；§4.3.2 ClassifyNode 注入 visualDescription；§4.3.3 UnderstandNode 加 currentSubProblem + 多小问感知段 + 后续小问简短复述；§4.3.4 / §4.3.5 加【StuckGuard 注入指令响应】段（FR-FALLBACK-001 5 问 + KNOWLEDGE_FALLBACK 落地）；§4.3.5 ExecuteNode 加显式 user prompt template + PROBLEM_BLOCKED 双条件触发约束（DR-012 mitigation）；§4.3.6 ReviewNode 加显式 user prompt template 传所有 subProblems）
**当前阶段**：详细设计 v0.1（19 节 + B3.1/B3.2/B4 全套下沉）+ 概要 v0.1.1 同步完成；**全部 P0/P1/P2 评审项闭环**；DR-010 / AR-012 概要侧均已闭环；待 muzi 终审进入实施期

---

## 1. 项目阶段进度

| 阶段 | 状态 | 文档 | 备注 |
|---|---|---|---|
| 本地环境搭建 | ✅ 已完成 | — | Supabase 本地栈 + dev server 起来；smoke 测过 |
| PRD v0.1 MVP | ✅ 已签发（草稿）| `doc/PRD/PRD_v0.1_MVP.md` | 17 个 US + 7 项 Agent 行为验收 |
| 需求分析 v0.1 | ✅ 已签发（草稿） | `doc/需求分析/需求分析_v0.1_MVP.md` | 22 个 FR + 12 个 NFR + 34 个 TC（B3.2 +TC-010a/b；B4 +TC-030/031/032）|
| 高层架构 v0.1 | ✅ 已签发（草稿）| `doc/高层架构/高层架构设计_v0.1_MVP.md` | 7 子系统 + 8 节点拓扑 + 10 个 D 决策 + B4 补充 §387 / AR-012 |
| **概要设计 v0.1 + v0.1.1 补丁** | ✅ **已签发（草稿）** | `doc/概要设计/概要设计_v0.1_MVP.md` | 表 schema + State Zod + API 契约 + 16 个 C 决策（v0.1 C1~C13 + v0.1.1 C14/C15/C16）；§4.2 新增 OcrSchema 多题契约；§4.1 state schema 加 subProblems / currentSubProblemIndex；§7.1 / §8.3 新增 subProblemRouter + insightLoopRouter 路由签名；§9.1 SSE chunk 6 种（新增 questions_detected + sub_problem_changed）|
| **测试验证策略 v0.1** | ✅ **已生效** | `doc/测试验证/测试验证策略_v0.1_MVP.md` | 六层验证 + Phase 推进表 + Hard Gate + L6 Agent Evals；沿用 rag-sdk ADR 0002 五层 + 加 L6 |
| **ADR Phase 1 落地** | ✅ **已完成** | 4 个 commit in main：`a5d1098` / `4f7c15d` / `a0cda58` / `284a225` | typecheck/check/verify scripts、ali-oss tsc shim、`.git/hooks/pre-commit`（vanilla shell + `scripts/install-hooks.sh`）、`.claude/settings.json` 4 条 hard gate hook |
| **详细设计 v0.1（B1~B4 全套）** | 🟡 **草稿待签发** | `doc/详细设计/详细设计_v0.1_MVP.md`（2900+ 行）+ `详细设计模版.md`（663 行）| 19 节齐全；B3.1 多题流程；B3.2 §4.3.4 D11【方向合理性自检】+ §14.4 E11；**B4 §4.3 各 PhaseNode prompt 下沉（VisionNode subProblems[] / PlanNode user template 加 currentSubProblem+insightPoints / ExecuteNode 扩 5 信号 + new_insight / ReviewNode 多小问汇总卡片）+ §5.2 PhaseSignalParse 扩 5 信号优先级 + §14.4 新增 E12/E13/E14（共 14 剧本）+ §14.4.1 Judge Rubric → NFR-BUSINESS 映射 + §15 D12/D13/D14 + §17 DR-012/DR-013**；待 muzi 终审 |
| **概要设计 v0.1.1 增量补丁** | ✅ **2026-05-21 已回补** | `doc/概要设计/概要设计_v0.1_MVP.md` §4.1 / §4.2 / §7.1 / §7.4 / §8.3 / §9.1 / §10 C14-C16 | DR-010 闭环 + AR-012 概要侧闭环 |
| 实施 | ⏳ 待启动 | 代码 | 主线 3~5 协作日，含真实使用 2 周验证 |
| MVP 上线验收 | ⏳ 待启动 | — | NFR-BUSINESS 三项指标 |

---

## 2. 文档地图

```
doc/
├── _项目状态.md                          ← 本文件（交接入口）
├── PRD/
│   ├── PRD模版.md                        通用模版（12 章 + §0 边界）
│   └── PRD_v0.1_MVP.md                   产品视角需求快照
├── 需求分析/
│   ├── 需求分析模版.md                    通用模版（17 章）
│   └── 需求分析_v0.1_MVP.md              FR/NFR/UC/TC/风险/估时
├── 高层架构/
│   ├── 高层架构设计模版.md                通用模版（13 章）
│   └── 高层架构设计_v0.1_MVP.md          系统结构 + 节点拓扑 + 决策
├── 概要设计/
│   ├── 概要设计模版.md                    通用模版(14 章)
│   └── 概要设计_v0.1_MVP.md              表 schema + State Zod + API 契约 + 13 C 决策
├── 测试验证/
│   └── 测试验证策略_v0.1_MVP.md          六层验证 + Phase 推进 + Hard Gate + L6 Agent Evals (新增)
└── 技术栈/
    └── 技术栈与版本_v0.1_MVP.md          全栈依赖清单 + 精确版本 + §8 决策反向锚点 + 11 节 (新增)
```

**职责边界**（每份文档 §0 都有详细说明）：
- **PRD**：产品视角行为契约（妹妹看到什么、Agent 说什么）
- **需求分析**：可估时 FR + 量化 NFR + 测试用例 TC + 风险登记
- **高层架构**：子系统拆分 + 节点拓扑 + 技术决策 + 部署
- **概要设计**：表 schema + State Zod + API 契约 + 模块接口签名 + RLS + SSE 协议
- **测试验证**：六层验证（L1–L5 阻断 / L6 观察）+ Phase 推进表 + Hard Gate + 覆盖率 90/90 + L6 Agent Evals 入门 14 剧本（B4 新增 E12/13/14）；测试相关的"唯一权威"
- **技术栈与版本**：全栈依赖 + 精确版本号快照 + 关联决策反向锚点；与 `package.json` / `supabase/config.toml` 同源迭代；不复述高层 §8 决策详情
- **详细设计**：Prompt 模板 + 算法 + 错误处理 + Runbook（待写）

---

## 3. 核心决策记录（按时间顺序）

### 概要层决策（概要设计 §10）

| ID | 决策 | 影响 |
|---|---|---|
| C1 | `elicit_conversations` +`has_resolved`/`current_phase`/`problem_type`；**不加**乐观锁 `version`（单用户并发为零） | §3.3 / AR-002 |
| C2 | `elicit_messages` +`phase`/`metadata jsonb` + 顺手修 3 个 POC 瑕疵（默认值 / nullable / UNIQUE） | §3.4 |
| C3 | State 5 新字段完整 Zod；`ocrResult` 改 optional 修复 POC bug | §4.1 |
| C4 | P-105 卡片 = `content`Markdown + `metadata.knowledgeCard` 镜像 JSON（D5 细化） | §4.2 |
| C5 | `compiledElicitGraph` 模块顶层单例 + lazy；不引入 `instrumentation.ts` 预热 | §8.1 |
| C6 | `isAdmin` ENV 一次性载入 Set + email 小写比对；`withAdminAuth` = `withAuth ∘ isAdmin` | §6.2 |
| C7 | **不实现** `POST /api/conversation/new`；沿用流式内 conversationNode + `data-custom` chunk | §5.1 |
| C8 | `/admin` 跨用户读 = 服务端 `/api/admin/*` + service_role client（不动 RLS 行策略） | §5.6 |
| C9 | `has_resolved` 双写 = 节点内 Drizzle transaction + chatNode 入口 reconcile 兜底（弱一致接受） | AR-002 闭环 |
| C10 | SSE 反代 6 条接口层必要约束（concrete nginx.conf 留详细 D2） | §9.3 |
| C11 | 业务表公共字段统一规约（7 必含：id/{biz}_id/version/is_deleted/created_at/updated_at/ext_info）+ Drizzle `commonAuditFields` helper；沿用 qian-quest；`user_id`/`metadata` 是业务字段不入 helper；MVP version/is_deleted 字段必含但业务逻辑不启用 | §3.2 |
| C12 | 枚举规范（DB smallint + TS `as const` + `createEnum`）；新建 `src/types/enums/base.ts` + `*.enum.ts`；废弃 POC `ChatMessageRoleEnum.ts` 的 TS `enum` 关键字；DB `role`/`current_phase`/`problem_type`/`phase`/`type` 全部 smallint | §2.4 / §10 C12 |
| C13 | `problem_type` 不加 DB CHECK 约束（最易演化的枚举字段，避免每加一个题型都要 ALTER TABLE；仅 TS Zod / ProblemTypeEnum 守门） | §3.4.3 / §10 C13；commit `a5d1098` 落地（迁移修复）|
| **C14** | **OcrSchema 改多题数组目标态**（v0.1.1 / B4 下沉）：questions[] 1~5 + selectedQuestionIndex? + isMulti + 每题 subProblems[] 静态拆分 | §4.2 / DR-010 闭环 / 详设 §15 D10 |
| **C15** | **state schema 加 B4 多小问字段**（v0.1.1）：state.subProblems[] (运行时态：status/insightPoints/stuckCountPerPhase/probedQuestionIds) + currentSubProblemIndex；原顶级 stuckCountPerPhase / probedQuestionIds 下沉到 SubProblemStateSchema 子字段 | §4.1 / AR-012 闭环 / 详设 §15 D14 |
| **C16** | **B4 双层循环条件边形态**（v0.1.1）：ExecuteNode 出口加 subProblemRouter（外层）+ insightLoopRouter（内层）2 个路由函数 + 不增新节点；新增 2 个 SSE chunk（questions_detected + sub_problem_changed）| §7.1 / §8.3 / §9.1 / 详设 §15 D12 / D13 |

### 产品层决策（PRD 阶段）

| ID | 决策 | 出处 |
|---|---|---|
| P1 | 学科范围：北师大版初中数学 初一~初三 | PRD §9 |
| P2 | 思路判断架构：宏观波利亚四阶段 + 微观方法论 menu | PRD §8 |
| P3 | 一会话 = 一题；多题选一 / 其余丢弃 | PRD §4 |
| P4 | 含图题不拒答；接受函数图像 / 统计图表题描述质量受限 | PRD §9 |
| P5 | 邮箱+密码注册登录（沿用 Supabase Auth） | PRD §4 |
| P6 | 家长后台仅只读，无配置入口 | PRD §10 |
| P7 | 已确认题目后点图片按钮：新会话弹 P-106 浮层；历史会话置灰 | PRD §6、US-016/017 |
| P8 | Claude 是开发主力，muzi 是产品负责人 + 评审者 | 需求分析 §4.3 |
| **P9** | **家长后台跨用户读展示原文，不做脱敏**（MVP 阶段家长 = muzi 本人，与妹妹同信任域；v0.2 引入更多家长时评估字段级 mask）| PRD §10 / 评审 P1-5 |

### 架构层决策（高层架构 §8 — 全部含问题陈述 + 备选退路）

| ID | 决策 | 影响 |
|---|---|---|
| D1 | Qwen-VL-Max 视觉 + DeepSeek 文本（成本省 5x，保留中文数学优势） | FR-VISION-*、FR-AGENT-* |
| D2 | ChatNode 拆 5 个 Pólya 阶段节点；守卫作 pre-check（不爆炸拓扑） | LangGraph 演化 |
| D3 | `hasResolved` state 主 + DB 镜像（checkpoint 时同步写） | FR-CONV-002 |
| D4 | 家长身份 = ENV `ADMIN_EMAILS` allowlist（仅 1 个家长） | FR-ADMIN-001 |
| D5 | P-105 卡片 = assistant message content 内嵌 JSON（不独立表） | FR-OUTPUT-001 |
| D6 | state 扩展 = 现 `ElicitGraphStateSchema` 加 5 字段 | LangGraph state |
| D7 | 阶段判据 = 关键词命中 + `phase_signal:"COMPLETED"` 协议（MVP 简单可行）| R-002 |
| D8 | OSS STS 沿用现 ARN，MVP D1 端到端验证 | R-005 |
| D9 | 前端状态 = Zustand + React 19 Compiler（POC 沿用） | FR-UI-* |
| D10 | LaTeX 渲染 = KaTeX（轻 80%） | R-006 |
| **D11** | **部署：自建 Supabase 全栈到阿里云 2C8G 轻量应用服务器**（妹妹在贵阳，Vercel + Supabase Cloud 不可用） | §10.2 |

### 已识别但推迟的产品决策（PRD §12 遗留）

- 思路对错强判断（US-007/008 完整能力）→ v0.2
- 错题本 / 知识点知识图谱 → v0.2
- 知识点体系结构化 → v0.3
- 多解法回顾 → 待决策
- 会话标题隐私边界 → 待决策
- 高阶解题技法（设而不求 / 同构法 / 韦达整体代换 / 障碍函数） → v0.2+
- 重视觉依赖题型引导精度（函数图像 / 统计图表）→ v0.2

---

## 4. 开放问题（概要 §13.2 重组后）

> 高层架构 §13.2 的 22 项中，概要阶段已闭环 10 项（→ 详见 §3 C1~C10 决策表）；剩余项 + 概要新发现合并重组如下。

### ✅ 概要设计已闭环（10 项）
对应 高层架构 #1/#2/#3/#4/#5/#6/#7/#10/#14/#21 — 回答见 `概要设计_v0.1_MVP.md §10` 的 C1~C10。

### 详细设计要回答（12 项 — 详细 D1~D2）
- 架构遗留：#8 Pólya 5 节点 prompt 模板；#9 VisionNode prompt；#10 StuckGuard 关键词规则；#11 5 问探路顺序；#12 DeviationGuard 算法；#13 OutOfScopeGuard 学科判定位置；#16 Daily cost log；#22 备用模型方案（AR-011）
- 概要新增：Nginx 完整 conf（C10 延续）；service_role client 文件 `src/lib/admin-db.ts` + `server-only` 校验（CR-004）；KnowledgeCard 前端 reader 版本兼容（CR-003/CR-005）；`withAuth` 启动日志（AR-003）

### 运维 / 部署 Runbook 要回答（6 项 — 详细 D3）
- 架构遗留：#15 全新部署 Runbook（含 `checkpointer.setup()` / `ADMIN_EMAILS` / OSS STS 检查）；#17 ECS 初始化 Runbook；#18 Supabase docker-compose 剪裁版；#20 Postgres pg_dump 备份；北师大初中数学知识点术语对照表
- 概要新增：`elicit_messages` 加 UNIQUE 前清理重复行的 SQL（CR-001）

### 流程 / 业务要回答（1 项）
- #19 ICP 备案 D1 启动（~20 工作日周期）

### 上游回补（B3.1 push back 后新增 1 项；2026-05-21 ✅ 全部闭环）
- ~~**概要 v0.1.1 增量补丁**（详设 §15 D10 / DR-010 闭环硬门控）~~ → 2026-05-21 已回补：
  - ✅ §4.2 新增 OcrSchema 完整目标态（`questions[]` 1~5 + `selectedQuestionIndex?` + `isMulti` + 每题 `subProblems[]`）
  - ✅ §9.1 SSE chunk 新增 `questions_detected` + `sub_problem_changed`（B4 同步加）
  - ✅ §7.1 / §8.3 节点签名新增 `shouldClassify` + `subProblemRouter` + `insightLoopRouter`（B4 同步加）
  - ✅ §4.1 state schema 新增 `subProblems[] / currentSubProblemIndex`（B4 同步加；C15）
  - ✅ §10 新增 C14 / C15 / C16 决策正文
  - ✅ §13.1 准入新增 4a / 14 / 15 项

### B4 跨文档评审发现（2026-05-21 / 全部闭环）
- ~~详设 §15 只到 D11、§17 只到 DR-011，但需求分析 / 高层架构已 B4 升级（FR-AGENT-008/009 / AR-012）→ B4 下沉缺口~~ → 已闭环：
  - ✅ 详设 §4.3.1 VisionNode prompt 加 `subProblems[]` 输出契约 + 拆分指令
  - ✅ 详设 §4.3.4 PlanNode user prompt 加 `currentSubProblem` + `insightPoints[]` 上下文（内层循环不重复同类提示）
  - ✅ 详设 §4.3.5 ExecuteNode prompt 扩 5 信号（COMPLETED/STAY/ESCALATE/SUB_PROBLEM_DONE/PROBLEM_BLOCKED）+ `new_insight` 追加破题点
  - ✅ 详设 §4.3.6 ReviewNode prompt 完成判据扩"所有 subProblems ∈ {done, blocked}" + 卡片 `subProblemSummaries[]`
  - ✅ 详设 §5.2 PhaseSignalParse 扩 5 信号 + 优先级（DONE > BLOCKED > ESCALATE > COMPLETED > STAY）
  - ✅ 详设 §14.4 新增 E12（单问多破题点）/ E13（多小问递进）/ E14（部分解卡死）+ §14.4.1 Judge Rubric → NFR-BUSINESS 映射
  - ✅ 详设 §15 新增 D12（双层循环条件边）/ D13（VisionNode 集成 Decomposition）/ D14（state 嵌套结构）
  - ✅ 详设 §17 新增 DR-012（B4 信号漂移）/ DR-013（拆分失误）
  - ✅ 详设 §18 准入新增第 21 / 22 项

### P0/P1/P2 评审项（2026-05-21）
- ✅ P0-1+P0-2 概要 v0.1.1 补丁（含 B4 state）
- ✅ P0-3 详设 §3.5 vs §4 开放问题 18 项核对（agent 全部确认完整）
- ✅ P0-4 §14.4.1 Judge Rubric → NFR-BUSINESS 映射
- ✅ P0-NEW 详设 B4 下沉
- ✅ P1-1 状态文档 / 需求分析计数（10→14 剧本；TC 34 个）
- ✅ P1-2 需求分析 FR-AGENT-003 已含 D11/DR-011 锚点（B4 升级时带入，无需补改）
- ✅ P1-3 has_resolved 双写时序图已有（详设 §13.2 + §13.4）
- ✅ P1-4 备用模型 prompt 兼容已含（详设 §8.2）
- ✅ P1-5 PRD §10 P9 admin 跨用户读不脱敏决策
- ✅ P2 PRD §10 加 5 条边界（流式发新消息禁用 / 多 tab / 会话超长 / SSE 重连 / 家长后台脱敏）+ 详设 §8.3 加异常 #14 / #15
- ⏳ 详设 §13.6 / §13.7 B4 时序图（非阻断，P3 nice-to-have 留 muzi 终审时按需补）

---

## 5. 架构层风险登记（高层架构 §12）

| 编号 | 风险（精简描述）| 严重度 | 决策时点 |
|---|---|---|---|
| AR-001 | LangGraph 节点共享逻辑过多反退化 | 中 | 详细设计 D2 |
| AR-002 | `hasResolved` 双写不一致 | 中 | ✅ 概要 C9 闭环（节点事务 + reconcile 兜底）|
| AR-003 | `ADMIN_EMAILS` ENV 改动需重启 | 低 | 详细设计 D1（启动日志）|
| AR-004 | qianwen-model.ts 实验代码污染 | 中 | MVP D1 清理 |
| AR-005 | OSS STS 角色信任策略未验证 | 中 | MVP D1 端到端测试 |
| AR-006 | LangGraph checkpoint 全新部署遗漏 setup() | 低 | 部署 Runbook |
| AR-007 | ~~Vercel 30s 超时~~ 已废弃（自建 ECS）| — | — |
| AR-008 | 阿里云轻量 ECS 单点故障 | 中 | MVP 上线前 + 备份策略 |
| AR-009 | ICP 备案 20 工作日 | 中 | MVP D1 启动备案 + 过渡方案 |
| AR-010 | Supabase 自建版本漂移 | 低 | 详细设计 D3 + 季度评估 |
| AR-011 | DeepSeek / Dashscope API 故障降级 | 中 | 详细设计 D2 备用模型 |

### 概要层新增风险（概要设计 §12）

| 编号 | 风险（精简描述） | 严重度 | 决策时点 |
|---|---|---|---|
| CR-001 | `elicit_messages` 加 UNIQUE 时历史重复行使迁移失败 | 低 | 迁移执行 D1（前置 DELETE） |
| CR-002 | PostgresSaver 与 Drizzle 各自连接池可能撞 Supavisor 限额 | 低 | MVP 上线后第 1 周观察 |
| CR-003 | `metadata jsonb` 演化时旧消息与新前端不兼容 | 中 | 详细 D2 前端 reader fallback |
| CR-004 | `withAdminAuth` 的 service_role client 误传到非 admin 路由 | 中 | 详细 D1（单文件 + server-only）|
| CR-005 | `data-custom` chunk 新增 `kind` 后旧 tab 不识别 | 低 | 详细 D2 前端 default 静默忽略 |
| CR-006 | `set_updated_at` 触发器与 checkpoint 镜像写撞车 | 低 | 接受（不参与业务逻辑）|

### 测试验证 ADR 落地新增风险（本会话）

| 编号 | 风险（精简描述）| 严重度 | 决策时点 |
|---|---|---|---|
| TR-001 | L6 evals judge 自身漂移（DeepSeek 升版后判分偏移）| 中 | Phase 3 启用 evals 时（§5.6 校准集 + golden 对齐）|
| TR-004 | 90/90 高水位驱动 Claude 写"水测试"（堆 expect(true).toBe(true) 凑数）| 高 | Phase 2 启用单测时（subagent prompt 禁水测试 + muzi 抽检断言强度）|
| **TR-007** | `.claude/settings.json` Hook 4（bypass-flag deny）的 grep 不剥离 quoted / heredoc，commit message 文本里出现 `--no-verify` 字面就会触发 deny | 中 | 详细设计阶段并行 follow-up（独立 small commit；见 §7.3）|
| **DR-010** | 详设 §4.3.1 / §9 / §13.4 / §15 D10 按 OcrSchema **目标态**写；概要 §4.2 / §9.1 / §7.1 现状是单数 sanitizedContent。实施期若先落详设 prompt 而未先回补概要 schema，OcrSchema.parse 会与 prompt 输出 questions[] 撞 | 高 | ① 已在 §4 登记"概要 v0.1.1 增量补丁待回补"；② 实施期 subagent 必须先回补概要再落 VisionNode；③ 详设 §18 准入第 19 项硬门控 | B3.1 评审通过后立即启动概要补丁 |
| **DR-011** | 阶段② PlanNode Prompt-only 弱判断（详设 §15 D11 落地）依赖 LLM 自检 C1 menu 内 + C2 非复述/补意图，可能漏判：① C1 对近义术语（"列方程"vs"列方程组"）判定模糊；② C2 对"复述"vs"自选方向"边界模糊 | 中 | ① 详设 §15 D11 已记录三段判据 + 备选退路（升级到 schema+prompt 双层方案 B）；② 详设 §17 DR-011 监测条款挂 L6 evals E1 + E11 平均分；③ 详设 §18 准入第 20 项已 ✅；④ Phase 3 evals 启用后任一剧本 ≥ 0.5 分回归两次触发升级 | B3.2 落地，Phase 3 evals 启用后监测 |
| **DR-012** | B4 双层循环信号 / 路由漂移：① ExecuteNode 5 信号 PhaseSignalParse 优先级误判；② subProblemRouter 误把 ESCALATE 信号当 'blocked'；③ insightLoopRouter 路由后 PlanNode 重复抛同类提示 | 中 | ① §5.2 5 信号优先级（DONE>BLOCKED>ESCALATE>COMPLETED>STAY）+ L3 单测覆盖；② §4.3.4 PlanNode 显式传 insightPoints；③ L6 E12/E13/E14 监测；任一回归 ≥ 0.5 分两次评审 | Phase 3 evals 启用后 |
| **DR-013** | VisionNode 集成 Decomposition 拆分失误：qwen-vl-max 误判单/多小问 / 小问 goal 提取错 | 中 | ① §4.3.1 prompt 加 few-shot 3 例边界；② L3 单测对 5 fixture 图断言 subProblems.length；③ L6 E13 sub_problem_changed 时机；④ 拆分准确率 < 80% → D13 备选退路 | Phase 3 evals 启用后 |

---

## 6. 协作模式硬约束（继承自记忆 [[feedback-collaboration-mode]]）

- **Claude 是开发主力**（前端 / 后端 / Agent 三层），**muzi 是产品负责人 + 评审者**
- 非小改动（>1 文件 / >50 行 / 涉及产品行为）必须走 **plan → muzi 评审 → 执行**
- 涉及 PRD §8 Agent 行为契约的修改必须**先对齐文档，再动代码**
- 落地决策需以注释 / PR 描述留痕

---

## 7. 下个会话第一步建议

### 7.1 本会话（2026-05-20 二次）已完成（归档摘要）

> ✅ **§7.1 入口任务全部闭环，无需再做**。下一会话直接跳到 §7.2 / §7.3。

| 工作 | 落地点 |
|---|---|
| §7.1 评审 commit `2f37a8d`（C1/C2/C3/C11/C12 代码落地）的 7 项结构核对 + lint/build 验证 | subagent 完成，7 项 PASS（含修 1 个 migration bug → commit `a5d1098`，对应 C13 落地）|
| 测试验证 ADR v0.1 MVP 起草 + 评审通过（覆盖率对齐 90/90）| `doc/测试验证/测试验证策略_v0.1_MVP.md`，commit `284a225` |
| ADR Phase 1 落地：typecheck/check/verify scripts + ali-oss tsc shim | commit `4f7c15d` |
| ADR Phase 1 落地：git pre-commit hook（vanilla shell）+ `scripts/install-hooks.sh` + README "Install Git Hooks" 步骤 | commit `a0cda58` |
| `.claude/settings.json` 4 条 Claude hard gate hook 配置 | commit `a0cda58`（hook live；已通过实际 commit 多轮验证）|
| 同步引用：`CLAUDE.md` / `README.md` / 本状态文档 | commit `284a225` |
| 项目目录规划评估 + Batch A/B/C/D 分批策略 | 见会话记录；目录优化推迟到详设期，见 §7.3.1 |
| Memory 新增 `feedback_strict_quality_baseline` | `~/.claude/projects/-Users-muzi-Project-elicit/memory/` |

<details>
<summary>下面是已归档的 §7.1 原始入口任务描述（保留作历史参考，不要再执行）</summary>

### 7.1.OLD 评审 + 提交「C2 / C12 代码落地」遗留改动

> 上一会话已经在工作目录里完成了 C2（`elicit_messages` 重构）+ C12（枚举规范）的代码落地，但**主线程未派 Sonnet subagent 评审、未跑 `pnpm build` / `pnpm lint`、未单独 commit**。这是本次会话 commit 时按用户级 CLAUDE.md 规则刻意留给下一轮的入口任务。

**改动文件清单（约 25 项）**：

| 类别 | 文件 | 落地的决策 / 工作 |
|---|---|---|
| 枚举重构（删 2 新增 5）| `src/types/enums/`（删 `ChatMessageRoleEnum.ts` / `ChatMessageTypeEnum.ts`；新增 `base.ts` / `chatMessageRole.enum.ts` / `chatMessageType.enum.ts` / `polyaPhase.enum.ts` / `problemType.enum.ts` / `methodCategory.enum.ts`）| C12（TS `as const` + `createEnum` 工厂；DB smallint 对齐）|
| Drizzle 公共字段 helper | `src/db/schema/_common.ts`（新增） | C11（`commonAuditFields`：7 字段）|
| Drizzle schema 重构 | `src/db/schema/conversation.ts` | C1 + C11（业务字段 `has_resolved` / `current_phase` / `problem_type` + 公共字段展开）|
| 手写迁移（不走 drizzle diff）| `supabase/migrations/20260520000001_elicit_conversations_v01_mvp_schema.sql` + `20260520000002_elicit_messages_v01_mvp_schema.sql` | C1 + C2 + C12 + C13（注意：§3.4.3 中 `problem_type` check 应已**移除**，按 C13 决策；评审时核对）|
| Supabase 类型重生 | `src/db/supabase/type.ts` | 跟随 schema 变化 |
| Mapper / Model 下游 | `src/db/models/ChatConversation.ts` / `ChatMessage.ts` / `src/db/mappers/ConversationMapper.ts`（如有） | 适配枚举类型 + 公共字段 |
| State schema | `src/agents/schemas/ElicitGraphStateSchema.ts` | C3 5 新字段（`currentPhase` / `problemType` / `stuckCountPerPhase` / `probedQuestionIds` / `lastDeviationAt`）+ Zod 数字字面量 union |
| Request DTO | `src/types/request/ChatMessageRequest.ts` | 枚举字段从 string 改 number |
| 前端 props / store / 组件 | `src/features/chat/props/*.ts` / `src/stores/useConversation.ts` / `src/features/chat/chat-*.tsx` | 适配枚举类型 + 字段更名 |
| OSS 路由（看起来与 C 决策无关）| `src/app/api/oss/sign-for-preview/route.ts` / `sign-for-upload/[conversationId]/route.ts` | 评审：是否是 §5.1 备注里说"两条未挂 withAuth 的补齐"？|
| 模型文件（POC 漂移）| `src/agents/models/qianwen-model.ts` | TD-001：顶层 `main()` + 硬编码 URL，可能本次会话已经被清理，也可能还没；评审确认 |
| `.gitignore` | 评审是否新增了不该被忽略的文件 |

**入口任务 prompt（开局贴这一段）**：

> 读 `/Users/muzi/Project/elicit/doc/_项目状态.md §7.1`。当前 working tree 里有上一会话遗留的 ~25 个未提交代码改动（C2 + C12 + C11 + C1 + C3 落地）。任务：
> 1. 派一个 Sonnet subagent（`model: sonnet` / `subagent_type: general-purpose`）做**代码评审 + 构建验证**：
>    - 评审重点：① 迁移 SQL 是否跟概要设计 §3.4 / §3.5 严格一致，**特别注意 §3.4.3 的 `problem_type` check 必须已按 C13 移除**；② 枚举重构后 `ChatMessageRole` / `ChatMessageType` 等是否所有调用点都改成 number；③ 公共字段 helper 是否被两张表都正确展开；④ State schema 5 新字段默认值是否齐全；⑤ Mapper 是否更新到使用新枚举 number 而非旧字符串；⑥ 前端是否还存在 snake_case 漏网（违反 §2.4 命名公约）；⑦ OSS 两条路由是否补了 `withAuth`
>    - 构建验证：`pnpm lint` + `pnpm build` 必须通过
>    - subagent 完成后返回评审报告 + 任何修正
> 2. 主线程评审 subagent 报告 + git diff 抽检关键文件
> 3. 跟我对齐 commit 范围后单独 commit（commit message 聚焦 C1 + C2 + C3 + C11 + C12 落地）
> 4. **顺带加入测试验证 ADR Phase 0 切入项**：在 `package.json` 的 scripts 中新增 `"typecheck": "tsc --noEmit"`，commit 一并带上；验证 `pnpm typecheck` 干净通过（对应测试验证 ADR §8 Phase 0 → Phase 1 的零成本切入）
> 5. （视情况）顺手清理 TD-001 `qianwen-model.ts` 顶层副作用 + TD-002 模型 ID 切换

</details>

### 7.2 主推荐：muzi 终审详设 v0.1（B1~B4 + v0.1.1 概要补丁全套）→ 进入实施期

**开局贴这一段**：

> 读 `/Users/muzi/Project/elicit/doc/_项目状态.md` §4。详设 v0.1 + 概要 v0.1.1 全套已就绪（B3.2 D11 阶段② 弱判断 + B4 双层循环下沉 / D12 / D13 / D14 + 评审 P0/P1/P2 全闭环）。
> 主推：muzi 终审 → 进入实施期。
>
> **实施期协作约束**：
> ① 用户级"落地代码必须派 subagent"硬规则仍然有效（model=sonnet）；纯文档（模版 / 设计文档 / 决策记录）主线程可直接写
> ② `.claude/settings.json` 4 条 hard gate hook 已 live。commit message 文本里**避免**出现 `--no-verify` / `--no-gpg-sign` 字面（hook 会误判；详见 §5 TR-007、§7.3.2 follow-up）
> ③ 任何 schema 改动（`src/db/schema/*`）会触发 PreToolUse ask 提示，**必须**配套迁移 + `pnpm supabase:type`
> ④ git pre-commit hook 会在每次 commit 跑 `pnpm typecheck && pnpm lint`，失败则阻断；subagent 完成代码落地前**必须**自己跑 `pnpm verify` 全绿才返回
> ⑤ **实施期硬门控**（详设 §18 准入第 19 项已闭环 ✅）：先回补概要 v0.1.1（已完成）→ 再落 VisionNode 代码；OcrSchema.parse 与 prompt 输出 questions[]+subProblems[] 已对齐

**实施期建议落地顺序**（从核心契约层往外）：
1. **代码骨架 + state schema**（详设 §15 D14 嵌套结构 / 概要 §4.1）：state schema 加 subProblems + currentSubProblemIndex；POC OcrSchema 一次性重写为目标态；TypeScript 全绿
2. **VisionNode + ClassifyNode**：按详设 §4.3.1 / §4.3.2 落 prompt + Zod parse；在 /resolve 二次 invoke 中初始化 state.subProblems
3. **PhaseNode（understand / plan / execute / review）+ 守卫 + 路由**（详设 §4.3 + §5 + §7.1 / §8.3）：包括 subProblemRouter / insightLoopRouter + PhaseSignalParse 5 信号优先级
4. **SSE chunk + 前端 store 分发**（详设 §9 + 概要 §9.1）：含 questions_detected + sub_problem_changed 双 chunk
5. **测试**（详设 §14）：L3 单测 + L4 fake-model demo + L5 smoke + L6 evals baseline
6. **部署 Runbook**（详设 §12）：阿里云 ECS 全栈搭建 + ICP 备案启动

**新会话注意**：§4.3.4 PlanNode prompt 完成判据已**非"任意方向即通过"**，含 D11 C1+C2 两条弱判断 + OTHER 题型例外，不要按旧版误读。

### 7.3 详设落地前的备选 / 并行 follow-up

下面 3 项都是**独立小任务**，可挑一个或多个并行 / 串行。每项预期 0.5~1 协作日。**§7.3.1 强烈建议先做**，其他可选。

#### 7.3.1 Batch C：详设落地前的目录骨架准备（**强烈建议先做**）

来自本会话「项目目录评估」结论：详设 D2 会拆出 Pólya 5 节点 + 3 个守卫节点，加上现有 4 个 = 12+ 平铺文件，先把骨架建好。

**开局贴这一段**：

> 派 Sonnet subagent（`model: sonnet` / `subagent_type: general-purpose`）做目录骨架准备，按以下 4 步独立 commit：
> 1. **节点分子目录**：`src/agents/nodes/` 按 `phases/` / `guards/` / `flow/` 分类。`phases/` 暂为空 placeholder（详设 D2 才创建 Pólya 5 节点）；`guards/` 同样为空（StuckGuard / DeviationGuard / OutOfScopeGuard）；`flow/` 移入现有 `ConversationNode.ts` / `OcrNode.ts` / `StartFinoutNode.ts`。`ChatNode.ts` 保留在 nodes/ 根并加 deprecated 注释（详设期 D2 拆掉）。改完更新 import 路径
> 2. **`prompt/` → `prompts/`**：单数改复数 + 对齐节点分类目录 `phases/` / `guards/` / `vision/`，现有 `OcrPrompt.ts` 移到 `prompts/vision/`
> 3. **补 `src/db/schema/message.ts`**：Drizzle schema 镜像 `supabase/migrations/20260520000002_*.sql` 的 elicit_messages 表结构，仅用于类型派生 + ADR §5.5 漂移检测；运行时不走（messages 走浏览器端 Supabase 客户端，不变）。注意 commonAuditFields helper 不展开（messages 的字段语义不同，需要单独定义）
> 4. **C9 reconcile 逻辑去向决策**：建 `src/agents/state/` 目录 + 一个 `reconcileHasResolved.ts` 空 placeholder（详设期填入），与 `db/mappers/` 区分语义边界
>
> 每步 commit 都必须通过 `pnpm verify`。subagent 完成后主线程评审 + commit。

#### 7.3.2 修复 Hook 4 精度 gap（TR-007）

`.claude/settings.json` Hook 4 用裸 grep 不剥离 quoted / heredoc，commit message 含 flag 字面会误触 deny。本会话已通过"重写文案绕过"短期解决，长期应修。

**开局贴这一段**：

> 派 Sonnet subagent 抽出独立脚本 `scripts/check-bypass-flags.sh`：用 sed 多 pass 剥离 single/double quote 和 heredoc body，再 grep token-level（前后 word boundary）。修改 `.claude/settings.json` Hook 4 调用该脚本（而非内联 grep）。验证矩阵：
> ① `git commit -m "foo --no-verify bar"`（quoted message） **不**触发
> ② `git commit --no-verify -m foo`（真 argv） **触发**
> ③ `cat <<EOF` heredoc 包含字面 **不**触发
> 完成后跑 `pnpm verify` 全绿，单独 commit。

#### 7.3.3 清理 AR-004 / TD-001（`qianwen-model.ts` 顶层副作用）

> 派 Sonnet subagent 清理 `src/agents/models/qianwen-model.ts`：移除顶层 `main()` 调用 + 移除硬编码 URL，改为标准 export。这是上会话 §7.1 step 5 的可选遗留，本会话未做。完成后跑 `pnpm verify` 全绿。

---

## 8. 项目代码现状（基于 git 与文件树，截至 2026-05-20）

- **分支**：`main`（领先 `origin/main` 4 个 commit，未 push）；本会话 4 个 commit：`a5d1098`（C13 fix）/ `4f7c15d`（typecheck script + ali-oss shim）/ `a0cda58`（pre-commit hook + Claude hooks）/ `284a225`（ADR + 同步引用）
- **可运行**：`pnpm dev` 起 dev server；`pnpm supabase:start` 起本地 Supabase（端口 54321/54322/54323）
- **质量基线**：`pnpm typecheck` 全绿 / `pnpm lint` 0 errors / 2 warnings（POC 遗留：`OcrNode.ts:12` 未用 `state` + `auth.ts:36` 未用 `e`）/ `pnpm build` 全绿
- **Phase 1 工具链**：`pnpm typecheck` / `pnpm check` / `pnpm verify` 已有；vitest 等 Phase 2 工具尚未引入
- **Hard gate**：
  - git pre-commit hook（`.git/hooks/pre-commit` + `scripts/pre-commit` + `scripts/install-hooks.sh`）— 新 clone 必须跑 `bash scripts/install-hooks.sh` 装一次
  - `.claude/settings.json` 4 条 Claude hook（schema-file ask / bypass-flag deny / git commit reminder / Stop unverified warn）已生效
- **已有节点**（POC，详设 D2 期会重构）：`OcrNode` / `ConversationNode` / `ChatNode` / `StartFinoutNode`（条件 fanout）—— 详设期会拆 ChatNode 成 Pólya 5 节点 + 加 3 守卫
- **已有 state schema**：`messages` / `userId` / `conversationId` / `questionImgUrl` / `hasResolved` / `ocrResult` + C3 新 5 字段（`currentPhase` / `problemType` / `stuckCountPerPhase` / `probedQuestionIds` / `lastDeviationAt`）—— 见 `src/agents/schemas/ElicitGraphStateSchema.ts`
- **已有迁移**：5 个文件
  - POC 期 3 个：`20260207151502_init_schema` / `20260208094748_create_messages_schma`(typo: schma) / `20260213051856_add_img_url_column_to_message_chema`(typo: chema)
  - MVP v0.1 期 2 个：`20260520000001_elicit_conversations_v01_mvp_schema` / `20260520000002_elicit_messages_v01_mvp_schema`
  - POC 迁移可考虑 MVP 上线前 squash（不紧急）
- **Drizzle schema**：仅 `src/db/schema/conversation.ts` + `_common.ts`（**缺 `message.ts`**，见 §7.3.1 step 3）
- **Phase 1 新增文件**：`scripts/pre-commit` / `scripts/install-hooks.sh` / `.claude/settings.json` / `src/types/oss-modules/policy2Str.d.ts`
- **现有问题待清理**（详细设计 D1 / 或 §7.3.3 follow-up）：`src/agents/models/qianwen-model.ts` 顶层 `main()` + 硬编码 URL（AR-004 / TD-001）

---

## 9. 环境配置（`.env.local` 已存在）

```
DEEPSEEK_API_KEY=已填
DASHSCOPE_API_KEY=已填（但模型 ID 将从 qwen-vl-ocr-latest 改为 qwen-vl-max）
POSTGRES_URL=本地 127.0.0.1:54322
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=已填 sb_publishable_*
OSS_*=全部已填（ARN: acs:ram::<ALIYUN_ACCOUNT_ID>:role/elicitossmanagerrole）
```
